const get = require('./lib/get');
const curry = require('./lib/curry');
const filter = require('./lib/filter');
const only = require('./lib/only');
const compose = require('./lib/compose');

module.exports = {
  get, curry, filter, only, compose,
};
